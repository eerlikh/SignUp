<h3> All the Users </h3>

<h4><%= link_to "Sign Up", new_user_path %></h4>

<ul>

  <% @users.each do |user| %>
    <li>
      <%= user.name %>
      <%= user.email %>

      <%= button_to 'Remove', user_path(@user), method: :delete %>
    </li>
  <% end %>

</ul>
